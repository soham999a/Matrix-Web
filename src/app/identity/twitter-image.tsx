import { MatrixOgImage } from "@/lib/og-image";

export const alt = "MATRIX — Identity. The visual foundation of an invisible architecture.";

export default function Image() {
  return MatrixOgImage({
    eyebrow: "Identity · Matrix",
    title: "The visual foundation of an invisible architecture.",
    subtitle: "Logo · Palette · Typography · Tokens",
  });
}
